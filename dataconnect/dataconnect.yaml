specVersion: "v1"
serviceId: "world-connect"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "world-connect-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
